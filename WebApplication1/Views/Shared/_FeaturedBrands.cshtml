@model IEnumerable<MobileShopConnection.HangSP>
<ul class="dropdown-menu">
    @foreach (var item in Model)
    {
        <li><a href="@Url.Action("Producer","Products",new { id = item.MaHang })">@item.TenHang</a></li>
    }
</ul>
